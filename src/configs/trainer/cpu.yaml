# CPU trainer configuration (for testing)

_target_: lightning.Trainer

max_epochs: 10
accelerator: "cpu"
devices: 1
precision: "32"

log_every_n_steps: 10
gradient_clip_val: 1.0
enable_checkpointing: true
enable_progress_bar: true
deterministic: false
